# 🚀 MVP 데모 실행 절차 (한 방 데모)

본 가이드는 Arch Risk Bot의 핵심 기능을 30초 내에 검증할 수 있는 재현 경로를 제공합니다.

## 1. 테스트 환경 준비
- [ ] GitHub App이 설치된 테스트 리포지토리 준비
- [ ] 로컬에서 `pnpm dev` (또는 배포된 서버) 실행 중 확인
- [ ] Smee.io 프록시 연결 활성화 확인

## 2. 데모 시나리오: "Hotspot & Syntax Violation"
아래 코드를 포함하는 새로운 파일을 생성하여 PR을 올립니다.

**대상 파일**: `risky_module.py`
```python
# 1. Syntax Error (분석기가 즉시 탐지)
def calculate_metrics(data)
    return [d * 2 for d in data]

# 2. Hotspot (거대 파일 시뮬레이션 - 800줄 이상의 로직 가정)
# ... (중략) ...
# 이 파일이 800라인이 넘는다고 가정할 때 아키텍처 스캐너가 'WARN'을 발생시킵니다.
```

## 3. 검증 포인트 (What to expect)
1. **신속한 코멘트**: PR 생성 후 30초 내에 `Arch Risk Bot`이 댓글을 남깁니다.
2. **리스크 요약**: 헤더에 "잠재적 리스크: N개"가 표시됩니다.
3. **가드레일 문구**: "이 봇은 코드는 자동으로 변경하지 않습니다" 문구가 포함되어야 합니다.
4. **Patch Suggestion**: Syntax Error에 대한 올바른 수정 코드(Diff)가 제시됩니다.
5. **아키텍처 경고**: 파일 규모가 클 경우 "Root is cluttered" 또는 "Hotspots" 경고가 요약에 포함됩니다.

## 4. FAQ
- **왜 댓글이 안 달리나요?**: Smee.io 연결이 끊어졌거나, `node_modules` 등 제외 폴더에 파일을 생성했는지 확인하세요.
- **분석 속도가 느려요**: 최초 분석 시 AI 모델 로딩 시간이 발생할 수 있습니다 (이후 캐싱 적용).
